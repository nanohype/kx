{
  "_README": "kx mirrors the eks-gitops addon catalog. `scripts/mirror-check.py check` compares every chart pinned here against eks-gitops at `upstream.ref`, in both directions, and fails on any undeclared difference. Move the pin with `sync`; ask whether it has fallen behind with `freshness` (scheduled, never a merge gate). Divergence is fine and must be declared below with a reason — an entry here is a decision on the record, an omission is not. `kind` is one of: gitops-only (upstream pins it, kx deliberately does not), kx-only (kx pins it, upstream deliberately does not), version (both pin it, at deliberately different versions).",
  "upstream": {
    "repository": "nanohype/eks-gitops",
    "path": "applicationsets",
    "ref": "614560ad84ad0461506c4574f50f7e449a7b3c55"
  },
  "divergences": [
    {
      "chart": "aws-load-balancer-controller",
      "kind": "gitops-only",
      "reason": "Provisions AWS ALBs from Ingress objects. kind has no ELB to provision; ingress-nginx plus Cilium's Gateway API fill the same role locally."
    },
    {
      "chart": "ingress-nginx",
      "kind": "kx-only",
      "reason": "The local stand-in for aws-load-balancer-controller. Different product, same role: something has to terminate Ingress on a cluster with no cloud load balancer."
    },
    {
      "chart": "external-dns",
      "kind": "gitops-only",
      "reason": "Writes Route53 records. There is no zone to write to on kind; a project that needs local DNS records adds coredns RFC2136 to the data slice."
    },
    {
      "chart": "karpenter",
      "kind": "gitops-only",
      "reason": "Provisions EC2 capacity in response to pending pods. kind nodes are containers on one host — there is nothing to provision."
    },
    {
      "chart": "kube-state-metrics",
      "kind": "gitops-only",
      "reason": "Present locally, not as its own chart: kube-prometheus-stack bundles kube-state-metrics and node-exporter as subcharts. Upstream runs it standalone because it has no in-cluster Prometheus to bundle it into."
    },
    {
      "chart": "operator",
      "kind": "gitops-only",
      "reason": "The eks-agent-platform operator. Both sides run it; only eks-gitops pins a chart version, because it installs the published chart from GHCR. kx installs the chart straight out of the sibling eks-agent-platform checkout and kind-loads an image it built from that same tree, since the dev image is not published — so there is no version here to compare against 0.6.3. That is the point of the local slice: it exercises the operator in the working tree, which a released artifact cannot. This entry appeared when the pin moved past the commit where eks-gitops started pinning the chart at all, not because either side changed what it does."
    },
    {
      "chart": "kube-prometheus-stack",
      "kind": "kx-only",
      "reason": "In-cluster Prometheus, Alertmanager, and Grafana standing in for Amazon Managed Prometheus and Amazon Managed Grafana. Upstream reaches those as external services, so it pins the pieces separately instead of the bundle."
    },
    {
      "chart": "argo-cd",
      "kind": "kx-only",
      "reason": "eks-gitops is ArgoCD's configuration, so it assumes an ArgoCD that already exists — the cluster factory installs it during bootstrap. kx has no bootstrap, so it installs ArgoCD itself and leaves it idle: present to work against, not owning the addon lifecycle."
    },
    {
      "chart": "trust-manager",
      "kind": "kx-only",
      "reason": "Distributes the self-signed CA bundle that local webhooks are issued from. Upstream issues from ACM and cert-manager against a real DNS zone, so it has no bundle to distribute."
    },
    {
      "chart": "cloudnative-pg",
      "kind": "kx-only",
      "reason": "In-cluster Postgres standing in for the Aurora clusters the tenant-substrate module provisions. A tenant declaring a relational datastore gets RDS upstream and a CNPG cluster here."
    },
    {
      "chart": "minio",
      "kind": "kx-only",
      "reason": "S3-compatible object storage standing in for S3, including as velero's backup target — same Velero chart on both sides, different backend."
    },
    {
      "chart": "nats",
      "kind": "kx-only",
      "reason": "Local queue and stream substrate standing in for SQS and Kinesis, so a tenant declaring a queue datastore has something to bind to."
    }
  ],
  "_crdInstallers_README": "eks-gitops installs some CRDs from a dedicated Application whose source is a git directory rather than a chart. Those carry no `chart` key, so the version comparison cannot see them — which is how kx went on fetching the argoproj.io CRDs from raw.githubusercontent.com at install time while every check reported a match. kx has no ArgoCD and no sync waves, so it reaches the same end by a different mechanism; each installer upstream must name that mechanism here. An entry is a decision on the record.",
  "crdInstallers": [
    {
      "appset": "argo-workflows-crds.yaml",
      "kinds": "argoproj.io",
      "mechanism": "stack/argo-platform/argo-workflows/values.yaml sets crds.full: false, so the chart renders all eight CRDs itself.",
      "why": "The chart default (crds.full: true) renders NO CRD manifests and instead applies them from raw.githubusercontent.com, making a local cluster bring-up depend on GitHub. eks-gitops solves the same problem with a separate Application at an earlier sync wave; kx has no waves, and crds.install: false alone would leave the cluster with no argoproj.io CRDs at all."
    },
    {
      "appset": "gateway-api-crds.yaml",
      "kinds": "gateway.networking.k8s.io",
      "mechanism": "stack/core/gateway-api-crds/install.sh applies the CRDs by server-side apply before cilium installs.",
      "why": "Cilium serves Gateway API locally and will not start with gatewayAPI.enabled when the CRDs are absent, so kx already ordered this by hand. Recorded here so the ordering is a declared answer rather than a coincidence. NOTE a real divergence: eks-gitops installs the STANDARD channel (config/crd/standard) and kx installs the EXPERIMENTAL one, because local development exercises routes the standard channel does not define. That also forces server-side apply — HTTPRoute's experimental schema exceeds the 256KB last-applied-configuration annotation limit."
    }
  ]
}
